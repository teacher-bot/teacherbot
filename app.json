{
  "name": "teacherbot",
  "description": "a collection of PRobot apps to assist trainers using GitHub repositories in the classroom",
  "keywords": ["PRobot", "node", "github", "training", "teaching"],
  "repository": "https://github.com/teacher-bot/teacherbot",
  "logo": "https://github.com/githubteacher.png",
  "scripts": {
  },
  "env": {
    "GITHUBTEACHER_TOKEN": {
      "required": true
    },
    "LANG": {
      "required": true
    },
    "RACK_ENV": {
      "required": true
    }
  },
  "formation": {
  },
  "addons": [

  ],
  "buildpacks": [
    {
      "url": "heroku/ruby"
    }
  ]
}
